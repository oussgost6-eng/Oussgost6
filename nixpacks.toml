[phases.setup]
nixPkgs = ["chromium", "chromedriver"]

[phases.install]
commands = ["pip install -r requirements.txt"]

[phases.build]
commands = []

[phases.start]
commands = ["python bot.py"]