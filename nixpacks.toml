[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
commands = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[phases.start]
commands = ["python bot.py"]